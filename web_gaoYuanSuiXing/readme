关于显示

原链接 http://crm.iappk.com/downloadApp.do?appid=2ea3dddc-fb31-416c-bd18-546c90a26311

这个页面将数据与格式分开. 整个页面只有 v_data 在取值, 而这个值应该是在后台处理返回给最终页面的.

其中 flag 项是判断 UA 的, "0"代表普通, "1"代表微信, "2"代表微博. 而判断过程并非使用 JavaScript, 可能是在服务器端运算的脚本语言.

而我们的判断是基于 JavaScript, 所以二者不能直接替换.